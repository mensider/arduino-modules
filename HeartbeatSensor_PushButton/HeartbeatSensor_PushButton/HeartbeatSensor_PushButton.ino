/*Demonstrates the working of Heartbeat sensor and PushbuttonBy Sooraj K*///Refer https://how2electronics.com/pulse-rate-bpm-monitor-arduino-pulse-sensor/#Source_Code//Refer https://microcontrollerslab.com/heart-beat-sensor-arduino/int HbeatPin = A0;  // choose the pin for the heartbeat sensorint buttonPin = 7;      // choose the input pin (for a pushbutton)int val = 0;        // variable for reading the pin statusint Hbeat_Pin = A0; // initializing the sensor output pin//initializng other variablesdouble alpha = 0.75;double change = 0.0;double oldValue = 0;double oldChange = 0;void setup() {  pinMode(buttonPin, INPUT_PULLUP);     // declare pushbutton as input  Serial.begin (115200);                // setting baud rate at 115200}void loop(){  val = digitalRead(buttonPin);         // read input value  if (val == HIGH) {                    // check if the input is HIGH (button released)    int rawValue = analogRead (output_Pin);                           // Reading the sensors values    double value = alpha * oldValue + (1 - alpha) * rawValue;         // calculating values using the formula    Serial.print (rawValue);            // printing the sensor output value on the screen    Serial.print (",");                                                                                              Serial.println (value);             // printing the heart beat value on the screen    oldValue = value;  }  delay(500);}